# Checkpoint 2026-07-19 (002)

**Дата:** 2026-07-19  
**Ветка:** `main` (отслеживает `origin/main`)  
**Последний коммит:** `35087b9 docs(checkpoints): add CHECKPOINTS_CONVENTION`  
**Рабочее дерево:** чистое  
**Текущая фаза:** Phase 7 завершена и запушена

---

## Что это за проект

Desktop-приложение для интерактивной оптимизации транскриптов и управления YAML-словарями очистки. Стек: Tauri 2 + React 19 + TypeScript + Vite + Monaco Editor + Zustand + Rust.

---

## Architecture map

| Concern | Path | Note |
|---|---|---|
| LLM crypto (Fernet) | `app/src-tauri/src/crypto.rs` | Чисто Rust, совместим с Python cryptography.fernet. |
| LLM streaming | `app/src-tauri/src/lib.rs` | Расшифровывает `base_url`/`api_key` и стримит SSE. |
| LLM types | `app/src/types/dictionaries.ts` | `LlmModelConfig`, `LlmYamlSettings` (модели + default_model). |
| LLM store | `app/src/store/llm.ts` | Карта моделей, выбор, add/remove/rename, миграция. |
| LLM UI | `app/src/components/SummaryView.tsx` | Селектор модели, баннер external, 🔒-индикаторы. |
| YAML editor | `app/src/lib/yaml-edit.ts` | `setLlmSettings` под новую схему. |
| Sample config | `sample/transcript_optimizer/settings.yaml` | 3 модели: minimax, openai, local_ollama. |
| Styles | `app/src/App.css` | `.security-warning`, `.model-selector`, `.crypto-input-row`. |
| Task conventions | `-=tasks=-/TASK_CONVENTIONS.md` | Адаптирован под датированную структуру. |
| Checkpoints convention | `-=CHECKPOINTS=-/CHECKPOINTS_CONVENTION.md` | Регламент чекпоинтов. |

---

## Implemented features (by phase)

- Phase 0: Tauri 2 + React 19 + TS + Vite + Monaco scaffold
- Phase 1: Dictionary editor (6 YAML files, validation, AST edits, undo)
- Phase 2: Transcript engine + viewer (parser, tokenizer, rules, OOV stats)
- Phase 3: Context menu → dictionary
- Phase 4: LLM summary (SSE streaming, Markdown/Mermaid, Diff)
- Phase 5: Dictionary helpers (OOV stats, collapse, similar-from, dedup)
- Phase 6: Markdown split-view editor
- **Phase 7: Multiple LLM models + Fernet encryption + external warning**

---

## Known gaps / NOT done

- UI шифрования использует `window.prompt` — рабочий, но не элегантный.
- Нет автоматического Rust → Python теста в CI (cross-проверка сделана вручную).
- `external` — булев флаг, не инферируется из URL.
- Нет ограничения TTL при расшифровке (соответствует Python без TTL).

---

## Key decisions & constraints

- **Fernet в чистом Rust**: `fernet` crate отказан, т.к. требует системный OpenSSL. Используем `aes` + `cbc` + `hmac` + `sha2` + `base64` + `rand`.
- **Порядок байт ключа**: как в Python cryptography — `key[:16]` = signing key, `key[16:]` = encryption key.
- **Расшифровка в Rust**: фронтенд не видит plaintext API-ключ; Fernet-ключ только в env.
- **Backward compatibility**: старый плоский `settings.llm` → `models.default`.
- **Task conventions**: перешли на датированную структуру (`YYYY-MM-DD/`) вместо фазовых директорий.

---

## Test status

```bash
cd app/src-tauri && cargo check              # OK
cargo test crypto::tests --lib                # 5 passed
cd app && pnpm exec tsc --noEmit             # OK
pnpm exec vite build                          # OK
```

Ручная проверка: Python-токен расшифровывается Rust; Rust-токен расшифровывается Python.

---

## Where to read more

1. `-=tasks=-/2026-07-19/20260719_001_model_selection_crypto_plan.md` — план Phase 7.
2. `-=PHASES=-/Phase_07.md` — итоги Phase 7.
3. `-=CHECKPOINTS=-/20260719_001_checkpoint.md` — предыдущий снапшот (до push).
4. `-=tasks=-/TASK_CONVENTIONS.md` — регламент задач.
5. `-=CHECKPOINTS=-/CHECKPOINTS_CONVENTION.md` — регламент чекпоинтов.
6. `LESSONS_LEARNED.md` — §3 про zustand-селекторы критичен.

---

## Next planned work

- Провести вживую smoke-тест через `pnpm tauri dev` (выбор модели, запуск саммари, шифрование).
- Опционально: улучшить UI шифрования с `window.prompt` на inline-форму/диалог.
- Опционально: добавить автоматический Rust ↔ Python тест в CI.
- Опционально: рассмотреть следующий элемент бэклога (см. `20260719_001_checkpoint.md` §«Возможные следующие шаги»).
